Für xgboost notwendiges Tool OpenMP
- Installieren von Homebrew Paketmanager

    `/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`
- OpenMP installieren

    `brew install libomp`


